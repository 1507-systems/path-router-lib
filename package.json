{
  "name": "@1507-systems/path-router-lib",
  "version": "0.1.1",
  "description": "Generic path-mount router primitives for Web-standard fetch handlers: mount matching, prefix-strip/exact-map rewrites, host redirects, constrained discovery payloads. Zero runtime deps, no Cloudflare types.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/1507-systems/path-router-lib.git"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
